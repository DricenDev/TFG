# This script is used to keep the container running
# so you can access it's bash terminal and run the 
# comunication multiple times, intead of a single
# execution per build.

import time
while True: time.sleep(1000)
